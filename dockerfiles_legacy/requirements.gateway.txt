# requirements.gateway.txt

# 1. The Web Server Framework
fastapi

# 2. The ASGI Server to run FastAPI
uvicorn[standard]

# 3. The HTTP client to call other services
httpx

# 4. Contract validation at gateway boundaries
pydantic

# 5. Async file I/O for canonical PDF writes
aiofiles

# 6. Multipart form data parsing (required for UploadFile / File() parameters)
python-multipart

# past reqs had flask - watch for need, but currently removed to start with clean env